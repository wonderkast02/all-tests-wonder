# Drive GPU Lab 0.1.0-dev.1 artifact manifest

The binary/source archives for this developer build are published as GitHub
prerelease assets, not committed to Git history.

Their expected SHA-256 values are recorded in `SHA256SUMS.txt`.

The authoritative source tree for this bootstrap is the verified
`Drive-GPU-Lab-0.1.0-dev.1-github-tree.zip`. GitHub Actions independently rebuilds platform artifacts
from the committed source tree.
