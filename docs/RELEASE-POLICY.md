# Release policy

Development artifacts use descriptive development/candidate versions and are
published as prereleases until the relevant qualification gates have passed.

A release must identify:

- exact source commit;
- CI result;
- artifact SHA-256 digests;
- supported/validated platform scope;
- known limitations.

Generated archives belong in GitHub release/CI assets rather than ordinary Git
history whenever practical.
